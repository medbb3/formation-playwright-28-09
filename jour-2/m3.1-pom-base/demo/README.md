# Démo M3.1

```powershell
npm install
npx playwright install chromium
npx playwright test
```

Simulation d'un changement d'interface : dans `pages/LoginPage.ts`, remplacer `'Login'` par `'Sign in'` et relancer `apres.spec.ts`.
