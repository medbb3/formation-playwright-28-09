# Démo M2.2

```powershell
npm install
npx playwright install chromium
npx playwright test
npx playwright test --grep @smoke
npx playwright test --grep-invert @smoke
npx playwright show-report      # ouvrir le test "capture masquée", onglet Attachments
```
