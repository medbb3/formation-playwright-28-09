# Point de départ — exercice M6.3

```powershell
npm install
npx playwright install chromium
npx playwright test
```

Fourni : `playwright.config.ts` (baseURL sur The Internet, `acceptDownloads`, trace en cas d'échec) et `fixtures/session.html`, page de session bancaire de 5 minutes avec alerte à une minute.

À écrire : `tests/fichiers.spec.ts`, selon `exercice.md`.
