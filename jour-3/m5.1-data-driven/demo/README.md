# Démo M5.1

```powershell
npm install
npx playwright install chromium
npx playwright test
npx playwright test --grep "Backpack"
```

Pour l'échec volontaire : modifier un prix dans `data/produits.csv`.
