# Matériel M7.2

```powershell
npm install
npx playwright install chromium
npm run agents:init
npx playwright test --project=standard      # commande.spec.ts et seed verts
npx playwright test --project=the-internet  # casse/login.spec.ts : 3 échecs
```
